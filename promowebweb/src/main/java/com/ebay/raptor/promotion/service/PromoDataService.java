package com.ebay.raptor.promotion.service;

import org.springframework.stereotype.Service;

import com.ebay.app.raptor.promocommon.CommonLogger;
import com.ebay.raptor.promotion.controller.PromotionController;

@Service
public class PromoDataService extends BaseDataService{
    private static CommonLogger logger =
            CommonLogger.getInstance(PromoDataService.class);
    
    
}
